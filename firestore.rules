rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Browser clients have no direct catalog access. All operations go through
    // authenticated server code, which also enforces the administrator allowlist.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
